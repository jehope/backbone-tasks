/*global define*/

define([
    'jquery',
    'backbone'
], function ($, Backbone) {
    'use strict';

    var TaskRouter = Backbone.Router.extend({
        routes: {
        }

    });

    return TaskRouter;
});
